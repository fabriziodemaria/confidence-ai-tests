# Confidence Python Example

This example demonstrates how to use the Confidence SDK together with the OpenFeature Python client to fetch a string flag value. Update `CLIENT_SECRET`, `FLAG_KEY`, and other placeholders in `main.py` with values from your Confidence project, then run `python main.py` to retrieve the configured flag output.
