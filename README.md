# Confidence OpenFeature Example

This project shows a minimal Python script that wires the Confidence SDK into OpenFeature, retrieves a string flag property, and prints its evaluated value for a sample user context.
